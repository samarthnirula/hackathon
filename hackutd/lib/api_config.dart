// lib/api_config.dart
class ApiConfig {
  // For CoinGecko (no key needed)
  static const String baseUrl = 'https://api.coingecko.com/api/v3';

  // If using an API that needs a key, you can define it here:
  // static const String apiKey = 'YOUR_API_KEY';
}
